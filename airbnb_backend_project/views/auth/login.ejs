<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>airbnb | Login</title>
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body>
    <%- include("../partials/header.ejs") %>
    <section
      class="border border-teal-200 shadow-md shadow-teal-200 max-w-xl p-4 mx-auto mt-16"
    >
      <h1 class="text-center text-lg mb-3">Login to airbnb</h1>

      <form action="/auth/login" method="post" class="flex flex-col gap-4">
        <div class="flex flex-col space-y-1">
          <label for="email" class="text-sm text-gray-500">Email</label>
          <input
            type="email"
            name="email"
            id="email"
            placeholder="example@gmai.com"
            required
            class="border border-teal-500 w-full px-4 py-3 rounded outline-none"
          />
        </div>
        <div class="flex flex-col space-y-1">
          <label for="password" class="text-sm text-gray-500">Password</label>
          <input
            type="password"
            name="password"
            id="password"
            placeholder="*******"
            required
            class="border border-teal-500 w-full px-4 py-3 rounded outline-none"
          />
        </div>

        <button
          type="submit"
          class="bg-teal-500 w-full px-4 py-3 rounded cursor-pointer duration-300 hover:bg-teal-900 hover:text-white"
        >
          Login
        </button>
      </form>
    </section>
  </body>
</html>
